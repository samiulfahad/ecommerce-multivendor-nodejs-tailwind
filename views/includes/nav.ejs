<!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-mainbg">
    <!-- Logo -->
    <a class="navbar-brand navbar-logo" href="/">Samiul Fahad</a>
    <!-- Collapse Button -->
    <button 
      class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <i class="fas fa-bars text-white"></i>
    </button>
    <!-- Links -->
    <div 
      class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto mx-auto">
            <!-- For Styling -->
            <div class="hori-selector">
              <div class="left"></div>
              <div class="right"></div>
            </div>
            <!-- Nav Links -->
            <% if(path==='404' || path === '500' || path === 'error') { %>
            <li class="nav-item active">
                <a class="nav-link" href="/"><%=path%> => Go Home <i class="fa-solid fa-home"></i></a>
            </li>
            <% } %>
            <% if(path==='/reset-password') { %>
            <li class="nav-item active">
                <a>Reset Password</a>
            </li>
            <% } %>
            <% if(path==='/new-password') { %>
            <li class="nav-item active">
                <a>Set Password</a>
            </li>
            <% } %>
            <% if(path==='/checkout') { %>
            <li class="nav-item active">
                <a class="nav-link" href="/"> Checkout <i class="fa-solid fa-cart-shopping"></i></a>
            </li>
            <% } %>
            <% if(path==='/seller/login') { %>
            <li class="nav-item active">
                <a class="nav-link" href="/"> Seller Login <i class="fa-solid fa-right-to-bracket"></i></a>
            </li>
            <% } %>
            <% if(path==='/seller/signup') { %>
            <li class="nav-item active">
                <a class="nav-link" href="/"> Seller Signup <i class="fa-solid fa-user-plus"></i></a>
            </li>
            <% } %>
            <li class="nav-item <%= path === '/shop' ? 'active text-primary' : ''%>">
                <a class="nav-link" href="/"> <i class="fa-solid fa-store"></i>Shop</a>
            </li>
            <% if(isCustomer && isLoggedIn) { %>
            <li class="nav-item <%= path === '/cart' ? 'active' : ''%>">
                <a class="nav-link" href="/cart"> <i class="fa-solid fa-cart-shopping"></i>Cart</a>
            </li>
            <li class="nav-item <%= path === '/wishlist' ? 'active' : ''%>">
                <a class="nav-link" href="/wishlist"> <i class="fa-regular fa-heart"></i>Wishlist</a>
            </li>
            <li class="nav-item <%= path === '/orders' ? 'active text-primary' : ''%>">
                <a class="nav-link" href="/orders"> <i class="fa-solid fa-truck"></i>Orders</a>
            </li>
            <li class="nav-item mr-5 <%= path === '/customer/account' ? 'active text-primary' : ''%>"">
                <a class="nav-link" href="/customer/account"> <i class="fa-regular fa-user"></i>Account</a>
            </li>
            <li class="main-header__item ml-5">
                <form action="/logout" class="mt-2" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit" class="btn btn-danger">Logout</button>
                </form>
            </li>
            <% } %>

            <% if(isSeller && isLoggedIn) { %>
            <li class="nav-item">
                <a class="nav-link <%= path === '/seller/account' ? 'active text-primary' : ''%>" href="/seller/account"> <i class="fa-solid fa-cart-shopping"></i>Account</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/seller/products"> <i class="fa-regular fa-heart"></i>My Products</a>
            </li>
            <li class="nav-item <%= path === '/seller/orders' ? 'active text-primary' : ''%>">
                <a class="nav-link" href="/orders"> <i class="fa-solid fa-truck"></i>Orders Received</a>
            </li>
            <li class="main-header__item">
                <form action="/logout" class="mt-2" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit" class="btn btn-danger">Logout</button>
                </form>
            </li>
            <% } %>

            <% if(!isLoggedIn) { %>
            <li class="nav-item <%= path === '/login' ? 'active text-primary' : ''%>">
                  <a class="nav-link" href="/login"> <i class="fa-solid fa-right-to-bracket"></i>Login</a>
            </li>
            <li class="nav-item <%= path === '/signup' ? 'active text-primary' : ''%>">
                <a class="nav-link" href="/signup"> <i class="fa-solid fa-user-plus"></i>Signup</a>
            </li>
            <% } %>
           
        </ul>
    </div>
  </nav>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-Piv4xVNRyMGpqkS2by6br4gNJ7DXjqk09RmUpJ8jgGtD7zP9yug3goQfGII0yAns" crossorigin="anonymous"></script>
  <script src="/js/nav.js"></script>